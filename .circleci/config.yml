# CircleCI configuration file
version: 2.1

jobs:
  build:
    docker:
     - image: circleci/node:10
    steps:
      - run: echo "Say hello to YAML!"
      - run: npm -v
      - run:
          name: Install
          command: yarn install
      - run:
          name: Build
          command: yarn build