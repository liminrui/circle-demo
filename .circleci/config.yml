# CircleCI configuration file
version: 2.1

jobs:
  build:
    docker:
     - image: cimg/node:16.20.1
    steps:
      - run: echo "Say hello to YAML!"
      - run: npm -v
      - checkout
      - run:
          name: Install
          command: npm install
      - run:
          name: Build
          command: npm run build